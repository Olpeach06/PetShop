<Page x:Class="PetShop.Pages.CartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CartPage">

    <Grid Background="#FF5F344C">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Список товаров -->
        <ListView x:Name="lvCartItems" Grid.Row="0" Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Width="750">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>

                        <!-- Изменённое отображение изображения: используем CurrentPhoto -->
                        <Image Grid.Column="0" Source="{Binding PRODUCTS.CurrentPhoto}" 
                               Height="80" Width="80" Stretch="UniformToFill"/>

                        <TextBlock Grid.Column="1" Text="{Binding PRODUCTS.name}" 
                                   VerticalAlignment="Center" Margin="10,0,0,0"/>

                        <TextBlock Grid.Column="2" Text="{Binding quantity}" 
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>

                        <TextBlock Grid.Column="3" Text="{Binding PRODUCTS.price, StringFormat={}{0:C}}" 
                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>

                        <Button Grid.Column="4" Content="Удалить" Background="red" Foreground="White"
                                Click="BtnRemove_Click" Tag="{Binding basket_id}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Итого и кнопка оформления заказа -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <TextBlock x:Name="txtTotal" FontSize="16" FontWeight="Bold" Margin="0,0,20,0"/>
            <Button Content="Оформить заказ" Background="#FF75425E" Foreground="#FFECC286"
                    Padding="20,5" FontWeight="Bold" Click="BtnCheckout_Click" BorderBrush="Black"/>
        </StackPanel>
        <Button Content="Назад" HorizontalAlignment="Left" Margin="10,12,0,0" Grid.Row="1" VerticalAlignment="Top" Width="107" Click="Button_Click" BorderBrush="Black" Background="#FF75425E" Foreground="#FFECC286" FontWeight="Bold"/>
    </Grid>
</Page>